# Button secondary

Extension final esperada si se usa pack externo: `.webp`

Decision recomendada: usar `FilledButton.tonal` o `OutlinedButton` tematizado en Flutter. No generar con IA.

Uso: acciones secundarias, repetir, volver o pista.
